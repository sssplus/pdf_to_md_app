# Backend dependencies for the Doc2MD Converter API.
# Pinned to tested minor versions (not just a floor) so a routine rebuild
# can't silently pull in an unaudited release of a library that parses
# untrusted public uploads (markitdown in particular). Bump deliberately,
# after testing, rather than letting `pip install` float to newest-on-PyPI.
fastapi~=0.138
uvicorn[standard]~=0.49
python-multipart~=0.0.32
markitdown[pdf,docx]~=0.1.6

# Compression. pikepdf is the pip-installable PDF fallback (qpdf bundled in the
# wheel); Ghostscript is an optional system binary used when present for better
# results. Pillow (already pulled in by markitdown) re-encodes DOCX images.
pikepdf~=10.9
Pillow~=12.2
