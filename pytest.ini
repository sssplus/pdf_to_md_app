[pytest]
# main.py and compression.py live at the repo root; add it to sys.path so the
# tests import them regardless of how pytest is invoked (`pytest` vs `python -m`).
pythonpath = .
testpaths = tests
